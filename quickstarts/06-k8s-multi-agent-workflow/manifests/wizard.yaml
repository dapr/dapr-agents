apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: wizard
  name: wizard
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wizard
  template:
    metadata:
      labels:
        app: wizard
    spec:
      containers:
      - image: localhost:5001/wizard:latest
        imagePullPolicy: Always
        name: wizard
